/**
 * Base path for the Roles settings in the Strapi admin.
 * Must include /admin since the admin app is a SPA at that path.
 */
export const ROLES_BASE = "/admin/settings/better-auth/roles";
export const ROLES_NEW = `${ROLES_BASE}/new`;
