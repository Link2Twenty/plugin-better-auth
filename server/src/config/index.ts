/**
 * Plugin configuration schema
 *
 * Users can configure Better Auth through Strapi's plugin configuration
 * in config/plugins.ts
 */
export default {
  default: {
    debug: false,
    betterAuthOptions: {},
  },
  validator() {},
};
